# games-web-platform (bootstrap)

This module is a bootstrap placeholder for the future web branch:

- Spring Boot backend
- WebSocket/REST adapters
- React frontend integration

It currently depends on `games-shared-core` so web transport and UI can reuse the shared game engine/contracts.
